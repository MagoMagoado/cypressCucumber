{
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": [
      "cypress/src/steps/**/*.js"
    ]
  },
  "scripts": {
    "test": "cypress run",
    "test:ci/cd": "cypress run --env tags='not @local'"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^24.0.1",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.8",
    "cypress": "^15.17.0"
  }
}
